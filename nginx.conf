events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # API proxy to cimeika-api service
        location /api/ {
            proxy_pass http://cimeika-api:8790/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # CORS headers
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
            add_header Access-Control-Allow-Headers "Content-Type, Authorization";
        }

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        # Static files with caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Main app

        # ── Legacy aliases → canonical (301) ─────────────────────────────
        location = /podija   { return 301 /podiya; }
        location = /nastrij  { return 301 /nastriy; }
        location = /calendar { return 301 /kalendar; }
        location = /gallery  { return 301 /galereya; }

        location / {
            try_files $uri $uri/ /index.html;
        }

        # SPA fallback
        location /podiya {
            try_files $uri $uri/ /index.html;
        }

        location /kazkar {
            try_files $uri $uri/ /index.html;
        }

        location /nastriy {
            try_files $uri $uri/ /index.html;
        }

        location /malya {
            try_files $uri $uri/ /index.html;
        }

        location /kalendar {
            try_files $uri $uri/ /index.html;
        }

        location /galereya {
            try_files $uri $uri/ /index.html;
        }

        location /ci {
            try_files $uri $uri/ /index.html;
        }
    }
}
